# Keybinds para Layouts de Dwindle y Master

binde = $mainMod, Q, killactive,
bind = $mainMod SHIFT, Q, exec, hyprctl activewindow | grep pid | tr -d 'pid:' | xargs kill -KILL
bind = $mainMod ,SPACE, togglefloating,
bind = $mainMod ,I, exec, ags inspect
bind = $mainMod ,P, exec, ags toggle Panel
bind = $mainMod, D, exec, $menu
bind = $mainMod SHIFT, O, exec,$pathScripts/blur.sh
bind = $mainMod CONTROL , S, exec,$pathScripts/hyprsunset.sh
bind = $mainMod SHIFT , S, pseudo, # dwindle
bind = $mainMod, S, togglesplit, # dwindle
bind = $mainMod SHIFT ,W, exec, sh $pathScripts/reload_waybar.sh
bind = $mainMod, W, exec, ags toggle Sidebar # Toggle hide/show waybar
bind = $mainMod, F, fullscreen
bind = $mainMod SHIFT,M, exec, sh $pathScripts/power_menu.sh

bind = $mainMod, C, exec, $terminal --title=clipse -e 'clipse'

# Move focus with SUPER + h j k l keys

bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Swap windows con SHIFT y los keys de h j k l
bind = $mainMod SHIFT, H, swapwindow, l
bind = $mainMod SHIFT, L, swapwindow, r
bind = $mainMod SHIFT, K, swapwindow, u
bind = $mainMod SHIFT, J, swapwindow, d

# Hyprshot

bind = , PRINT, exec, ~/scripts/screenshot.sh

# Workspace
bind = $mainMod SHIFT,TAB, workspace, m+1
bind = $mainMod , tab, cyclenext

# Cycle windows if floating bring to top
bind = ALT, tab, cyclenext
bind = ALT, tab, bringactivetotop  
